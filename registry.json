{
  "total_tools": 4,
  "tools": [
    {
      "name": "crypto_price_checker",
      "display_name": "Crypto Price Checker",
      "description": "Fetches live cryptocurrency market prices, 24h change, and market cap in USD using the free CoinGecko API.",
      "category": "Finance",
      "language": "python",
      "mcp_compatible": true,
      "author": {
        "name": "Tarun Jalandhara",
        "github": "tarunjandra"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "coin_id": {
            "type": "string",
            "description": "Cryptocurrency ID (e.g. bitcoin, ethereum, solana, dogecoin)",
            "default": "bitcoin"
          },
          "currency": {
            "type": "string",
            "description": "Target fiat/crypto currency (default 'usd')",
            "default": "usd"
          }
        },
        "required": [
          "coin_id"
        ]
      },
      "env_vars": []
    },
    {
      "name": "duckduckgo_search",
      "display_name": "DuckDuckGo Search",
      "description": "Searches the web via DuckDuckGo and returns top search results including titles, snippets, and URLs.",
      "category": "Search & Web",
      "language": "python",
      "mcp_compatible": true,
      "author": {
        "name": "Tarun Jalandhara",
        "github": "tarunjalandhara"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Search query keywords"
          },
          "max_results": {
            "type": "integer",
            "description": "Maximum number of search results to return (default 5)",
            "default": 5
          }
        },
        "required": [
          "query"
        ]
      },
      "env_vars": []
    },
    {
      "name": "slack_notifier",
      "display_name": "Slack Notifier",
      "description": "Sends rich markdown notifications and webhook alerts directly to Slack channels.",
      "category": "Communication",
      "language": "python",
      "mcp_compatible": true,
      "author": {
        "name": "Tarun Jalandhara",
        "github": "tarunjalandhara"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "webhook_url": {
            "type": "string",
            "description": "Slack Incoming Webhook URL (or reads SLACK_WEBHOOK_URL env var)"
          },
          "message": {
            "type": "string",
            "description": "Markdown formatted message to post"
          },
          "title": {
            "type": "string",
            "description": "Optional title/header block for notification"
          }
        },
        "required": [
          "message"
        ]
      },
      "env_vars": [
        "SLACK_WEBHOOK_URL"
      ]
    },
    {
      "name": "wikipedia_search",
      "display_name": "Wikipedia Knowledge Search",
      "description": "Searches Wikipedia and returns clean extract summaries, page titles, and canonical URLs for agent knowledge retrieval.",
      "category": "Search & Web",
      "language": "python",
      "mcp_compatible": true,
      "author": {
        "name": "Tarun Jalandhara",
        "github": "tarunjandra"
      },
      "parameters": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "Topic or title to search on Wikipedia"
          },
          "sentences": {
            "type": "integer",
            "description": "Number of summary sentences to return (default 3)",
            "default": 3
          }
        },
        "required": [
          "query"
        ]
      },
      "env_vars": []
    }
  ]
}